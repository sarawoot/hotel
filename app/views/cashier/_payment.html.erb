<div id="payment" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="payment_header" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="payment_header"><%=raw t("html.payment") %></h3>
  </div>
  
  
  <%= simple_form_for @input_type, url: check_out_payment_path(input_type_id: params[:input_type_id]),
                      :html => {:class => 'form-horizontal' } do |f| %>
  <div class="modal-body">
    
    
    
    
  </div>
  <div class="modal-footer">
    <%= submit_tag "#{t("html.confirm")}", class: "btn btn-primary"%>
    <button class="btn" data-dismiss="modal" aria-hidden="true"><%=raw t("html.close")%></button>    
  </div>
  <% end %>
</div>