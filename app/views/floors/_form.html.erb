<%= simple_form_for(@floor, :html => {:class => 'form-horizontal' }) do |f| %>

  <div class="form-inputs">
    <%= f.input :seq %>
    <%= f.input :name %>
    <%= f.input :used, :wrapper => :append   do %>
      <%= f.check_box :used, {},'1','0'  %>
    <% end %>
  </div>

  <div class="form-actions">
    <%= f.submit (@floor.new_record? ? "#{t "html.save"}"  : "#{t "html.update"}" ), class: "btn btn-primary" %>
      <%= link_to "#{t "html.back" }", floors_path, class: "btn" %>
  </div>
<% end %>